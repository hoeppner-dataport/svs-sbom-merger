on:
    workflow_dispatch:

jobs:
    test-my-action:
        name: Test the action
        runs-on: ubuntu-latest
        steps:
            - name: Summarise SVS-SBOMs
              uses: hoeppner-dataport/summarise-svs-sboms@ddc04b7b05bde7daa9cdb32193c604927f874e63
              with:
                  filename: dependencies.sbom.json
                  repos: |-
                      hpi-schul-cloud/schulcloud-client:999.6.6
                      hpi-schul-cloud/tldraw-server:999.6.6
                      hpi-schul-cloud/schulcloud-server:999.6.6
